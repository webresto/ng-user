import { AddAddressDirective } from './update-profile.directive';

describe('AddAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new AddAddressDirective();
    expect(directive).toBeTruthy();
  });
});
