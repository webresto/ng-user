import { UpdateProfileDirective } from './update-profile.directive';

describe('UpdateProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new UpdateProfileDirective();
    expect(directive).toBeTruthy();
  });
});
