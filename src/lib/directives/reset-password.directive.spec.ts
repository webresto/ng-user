import { ResetPasswordDirective } from './reset-password.directive';

describe('ResetPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
