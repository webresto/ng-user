import { SignUpDirective } from './sign-up.directive';

describe('SignUpDirective', () => {
  it('should create an instance', () => {
    const directive = new SignUpDirective();
    expect(directive).toBeTruthy();
  });
});
