import { BalanceDirective } from './balance.directive';

describe('BalanceDirective', () => {
  it('should create an instance', () => {
    const directive = new BalanceDirective();
    expect(directive).toBeTruthy();
  });
});
