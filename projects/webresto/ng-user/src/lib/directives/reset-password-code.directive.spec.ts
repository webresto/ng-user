import { ResetPasswordCodeDirective } from './reset-password-code.directive';

describe('ResetPasswordCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new ResetPasswordCodeDirective();
    expect(directive).toBeTruthy();
  });
});
