import { ToggleDishToFavoritesDirective } from './toggle-dish-to-favorites.directive';

describe('ToggleDishToFavoritesDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleDishToFavoritesDirective();
    expect(directive).toBeTruthy();
  });
});
